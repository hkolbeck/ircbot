To install, be sure that $GOROOT, $GOOS, and $GOARCH are all set as described at http://golang.org/doc/install.html#environment, and that the directory $GOROOT/pkg/$GOOS_$GOARCH/ exists, then run `make install`